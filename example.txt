program example(input, output);
var x, y: integer;
function gcd(a, b: integer): integer;
begin
	if b = 0 then gcd := a
	else gcd := gcd(b, a mod b)

	3.4E+
	3.E4+
	3E4+
	3.E

end;
begin
	read(x, y);
	write(gcd(x, y))
end.
